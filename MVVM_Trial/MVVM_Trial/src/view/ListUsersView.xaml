<Window x:Class="MVVM_Trial.src.view.ListUsersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVM_Trial.src.view"
        mc:Ignorable="d"
        Title="ListUsersView" Height="450" Width="800"
        FontSize="16">
    <StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Text="ID" HorizontalAlignment="Center" />
            <TextBlock Text="Name" Grid.Column="1" HorizontalAlignment="Center"/>
            <TextBlock Text="Job Title" Grid.Column="2" HorizontalAlignment="Center"/>

            <TextBlock Text="{Binding Path=ID}" Grid.Row="1" Grid.Column="0" />
            <TextBlock Text="{Binding Path=Name}" Grid.Row="1" Grid.Column="1" />
            <TextBlock Text="{Binding Path=JobTitle}" Grid.Row="1" Grid.Column="2" />
        </Grid>

        <Button x:Name="btnAddUser" HorizontalAlignment="Center">Add Users</Button>
    </StackPanel>
</Window>
